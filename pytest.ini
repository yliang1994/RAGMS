[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = .git .venv .pytest_cache data logs
addopts = -ra
markers =
    unit: fast, isolated unit tests
    integration: cross-module integration tests
    e2e: end-to-end workflow tests
